<!DOCTYPE html>
<html class="h-full transition-colors <%= cookies[:ui_theme] %>">
  <head>
    <title>SkillSetGo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= favicon_link_tag 'logo-base-32x32.png' %>
  </head>

  <body class="max-w-7xl min-w-[350px] mx-auto flex h-full w-full min-h-screen flex-col bg-background text-foreground">
    <%= render 'layouts/toasts' %>
    <%= render 'layouts/navbar' %>
    <div class='h-full w-full flex flex-col'>
      <%= yield %>
    </div>
    <% unless controller.controller_name == 'services' && controller.action_name == 'new' || session[:form] == 'preview' %>
      <%= render 'layouts/footer' %>
    <% end %>
  </body>
</html>