<% if current_user&.role&.name == 'freelancer' %>
    <%= render partial: 'users/freelancer', locals: { appointments: @appointments, services: @services, reviews: @reviews } %>
<% else %>
  <%= render "client", locals: { appointments: @appointments, reviews: @reviews} %>
<% end %>