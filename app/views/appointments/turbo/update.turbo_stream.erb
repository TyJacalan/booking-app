<%= turbo_stream.update 'toasts', partial: 'layouts/toasts' %>

<% if @status %>
  <%= turbo_stream.update "appointment_#{@appointment.id}", 
    partial: 'appointment', 
    locals: { appointment: @appointment } %>
<% end %>
