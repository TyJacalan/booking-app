<%= turbo_stream.prepend 'notification_list',
  partial: 'notification',
  locals: { notification: @notification } %>
<%= turbo_stream.update 'notification_trigger', locals: { count_unread: @count_unread } %>
