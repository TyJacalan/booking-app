#!/usr/bin/env bash

# Exit when any command fails
set -e

# Run RuboCop checks
rubocop

# Auto-correct offenses
rubocop -a

# Run the test suite
rspec

# Ensure seeds work
RAILS_ENV=test bundle exec rails db:reset
